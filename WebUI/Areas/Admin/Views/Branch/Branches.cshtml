@model WebUI.ViewModels.Branch.BranchesListViewModel

@{
    ViewBag.Title = "Отрасли службы поддержки";
    Layout = "~/Areas/Admin/Views/Shared/_cPanelLayout.cshtml";
}

<h2 class="text-center display-4 my-3 text-white">Отрасли службы поддержки</h2>
<div class="d-flex flex-row justify-content-between">
    <div class="flex-fill align-self-center">
        @Html.ActionLink("Новая запись", "AddBranch", null, new { @class = "btn btn-success" })
    </div>
    <div class="flex-fill align-self-center">
        @using (Html.BeginForm("Branches", "Branch", new { search = Model.Search }, FormMethod.Get, new { @class = "input-group", role = "form" }))
        {
            @Html.TextBox("search", Model.Search as string, new { @class = "form-control ml-1", @placeholder = "Поиск отрасли службы поддержки", @autocomplete = "off" })
            <div class="input-group-append">
                <input type="submit" value="Поиск" class="btn btn-info" />
            </div>
        }
    </div>
</div>
@Html.Partial("BranchesCardView")

